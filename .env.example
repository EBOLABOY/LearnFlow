# 深学助手认证系统环境变量配置
# 将此文件复制为 .env.local 并填入真实值

# ==================== 数据库配置 ====================
# SQLPub MySQL 数据库连接信息
DB_HOST=mysql2.sqlpub.com
DB_PORT=3307
DB_USER=your_username
DB_PASSWORD=your_password
DB_DATABASE=learnflow

# ==================== 认证配置 ====================
# JWT Token 密钥（建议使用32位以上随机字符串）
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters

# 用户注册邀请码（建议设置复杂密码）
INVITE_CODE=your_invitation_code_here

# ==================== Vercel 部署配置 ====================
# 生产环境API基础URL（部署后需要更新前端代码中的API_BASE_URL）
# 格式: https://your-project-name.vercel.app
VERCEL_URL=https://your-vercel-project.vercel.app

# ==================== 安全配置 ====================
# 允许的CORS源（生产环境建议限制具体域名）
CORS_ORIGIN=*

# ==================== 配置说明 ====================
# 1. 在SQLPub创建MySQL数据库，获取连接信息
# 2. 使用 database/init.sql 初始化数据库表结构
# 3. 生成强密码作为JWT_SECRET和INVITE_CODE
# 4. 在Vercel项目设置中添加这些环境变量
# 5. 部署完成后，更新前端代码中的API_BASE_URL为实际的Vercel URL